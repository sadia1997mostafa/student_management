spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/student_management
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
